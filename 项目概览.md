# Synergy - TCG/CCG 卡牌游戏规则引擎

双人卡牌对战游戏（TCG/CCG）的规则引擎框架，融合《游戏王》《万智牌》的核心机制。

## 游戏规则

### 区域分布
- 游戏以正六变形铺满场地，每个玩家分别具有 额外1格 卡组1格 墓地1格 除外区1格 生物召唤区域6格 非生物使用区域6格 元素池6格  
- 区域功能 额外放置最多15张额外怪兽，基于游戏王的特招规则见后续

### 费用体系 （代价系统）
- 红蓝绿灰四色元素是最基本的代价，卡牌的使用，效果的发动都需要支付代价。
- 元素是每回合准备阶段，玩家从手牌中挑选一张放进元素池，基于该卡的费用构成，放置对应数量的元素指示物，一回合一次，去除一个指示物，玩家获得对应元素，指示物消耗完毕后会直接进入墓地
- 红蓝绿划分效果集中功能倾向明确的部分，灰色是难以分类的，或通用的效果。黑白是预留拓展色，暂时不制作

#### 费用自动推导（配置表为唯一权威）
效果的元素代价不再逐卡硬编码，而是由配置表自动推导（`Effects/CostDerivation.cs` 的 `CostDerivationService.DeriveElementCosts`）：
- 单个原子效果元素费 = `round(BaseCost × CostMultiplier × max(1, 效果值))` 个元素，颜色取 `ElementAffinities.GetAffinityForEffect(type).PrimaryColor`（伤害→红、抽牌→蓝、回复→绿、难分类→灰）。
- `BaseCost`/`EffectColor`/`CostMultiplier` 全部来自 `AttributeValueConfig.json`，调平衡只改配置不改代码。
- 基准锚点：造成 1 伤害 / 抽 1 张 / 回收 1 张 / 回复 2 命 = 1 费 = 1 个对应颜色元素。`Heal` 系数为 0.5（回 2 命=1 费），`ReturnFromGraveyard`（复活）系数调高为 2.0。
- 卡牌只显式声明**特殊代价**（沉睡、召唤素材、弃牌等非元素代价）；元素代价由效果自动派生后与特殊代价合并支付。
- 接线点：`EffectExecutor.ExecuteAsync` 先派生元素代价再异步支付；`CanActivate` 第 4 步用 `CostHandlerRegistry.CanPayAll` + `CostOffsetService.CanAfford` 做可支付预检（抵消可"救活"激活）。
- **节点化费用规则（`DeriveElementCosts` 为 Steps 感知）**：配 `Steps` 的卡仅对**主序列原子**（`Kind==Atomic`）计费，分支 `then/else` 奖励一律 **0 费**（免费）；未配 `Steps` 的旧卡退化为扁平 `Effects` 线性计费（向后兼容）。
- **数量两态**：固定数量原子费用 `×N`（N=有效目标数，全部/任意按 1）；**动态数量**原子费用计 **0**，且 `ElementPool.AddCardToPool` 对含动态数量原子的卡返回 false（不可作地牌产元素，作为灵活使用的代价）。
- **检索按筛选维度计费**：`SearchDeck` 原子费用 = 筛选维度档系数（确切单卡/按名称=3、类型+种族=2、单一维度=1），维度档 id 存于该原子的 `StringValue`，系数来自 `Configs/BranchConfig.json`。
- **抽牌缺陷减费**：`DrawCard` 原子可叠加挂 0..2 个减费缺陷（`UnusableThisTurn`/`DiscardAtEndOfTurnIfInHand`，各 −1，下限 0），缺陷 id 存于该原子的 `Drawbacks`。
- **编辑器算费同步**：`Assets/UI/Data/CardCostCalculator.cs`（建议法力值启发式估值）也已 Steps 感知，对分支奖励 0 费、动态数量 0、固定 ×N、抽牌缺陷减费保持与运行时一致。

#### 代价抵消（无色抵扣，至少保留 1 点本色）
`Effects/CostOffset.cs` 的 `CostOffsetService.PayElementWithOffsetAsync` 在最终支付前弹抵消面板（复用 `TargetSelectionService` 异步选择）。每次抵消减 1 费（减 1 个所需元素），消耗对应资源，且**每种颜色被抵消数 ≤ 需求−1**（保证至少留 1 点本色元素）。4 种机制系数/单局上限来自 `Configs/CostOffsetConfig.json`：

| 机制 | 抵消 1 费消耗 | 单局上限 | CostType |
|---|---|---|---|
| 流失（无源失命） | 2 life | 14 费 | LifePayment |
| 弃手牌 | 1 张 | 6 费 | DiscardCard |
| 磨本组（deck 顶→墓） | 5 张 | 6 费 | MillDeck |
| 送额外组（extra→墓） | 3 张 | 5 费 | SendExtraDeck |

- 上限合计 14+6+6+5 = **31**，等于玩家初始资源度量，确保自洽。单局已用计数挂在 `Player`（`OffsetDrainUsed` 等 4 字段），`InitGame` 时 `ResetOffsetUsage()` 归零。
- AI / 无头 / 超时 / 无 UI → 不抵消，直接付元素；资源不足时贪心抵消兜底。
- 剩余元素经 `ElementPaymentValidator.GetPaymentPlan` 从 `AvailableMana` 扣（灰色任意付 / 本色付）。

#### 元素池生命周期（地牌→指示物→横置→入池→墓地）
`BaseSystems/ElementPool.cs` 为两层结构：`PooledCard.Tokens`（地牌上的元素指示物）+ `PlayerElementPool.AvailableMana`（可花费的池）。
- 地牌放入时按费用构成生成对应颜色指示物。每张地牌有 `IsTapped` 横置态。
- 取元素：选一张 `!IsTapped` 且有该颜色指示物的地牌 → 移 1 指示物入 `AvailableMana` → 该地牌 `IsTapped=true`（每地牌每回合横置一次）。
- **跨回合可取**：支付时无论轮到谁都可横置己方地牌取元素（已移除"仅回合玩家"限制）。
- `ResetTurnUsage` 在该玩家回合开始把其所有地牌恢复直立。
- 指示物耗尽的地牌直接进墓地（`CheckDepletedCards`/`MoveDepletedCard`）。
- 地牌位置无关，仍走 `Zone.ElementPool`，**不**绑定 HexCell（见待办）。

### 特招规则
一、融合召唤
核心条件：
关键词要求：必须使用至少一个带关键词的生物作为素材 
召唤方式：牺牲符合条件的素材生物，如果牺牲的费用小于要特招的随从，则支付剩余法力费用
独特优势：融合生物继承素材的关键词，重复可叠加关键词。比如：关键词，圣盾，抵挡一次伤害，融合生物本身具有圣盾，又融合了一个圣盾素材，此后它的具有的圣盾将抵挡两次伤害。

二、同步召唤
核心条件：
调整要求：必须使用至少一个"调整"生物，和场上至少一个非调整生物
费用门槛：作为素材的非调整生物的总费用必须大于同步怪兽的费用
时机灵活：调整生物可通过额外支付费用，提高自身效果发动速度，进行同步召唤时，将自身的效果发动速度赋予该同步怪兽。
独特优势：同步的召唤和效果，发动速度快，对手难以连锁响应

三，超量召唤
核心条件：
超量怪兽没有法力值，只能配置发动时扣除法力值的效果
素材叠放：将场上任意数量的生物作为超量素材置于超量怪兽下方特殊召唤
动态属性，攻击力/生命值 由超量素材的对应属性值总和决定
独特优势：发动效果时，可选择支付法力费用或拔除对应费用的超量素材替代，可将多个小生物转换为大生物，对于基于生物属性计算的效果具有优势


四、链接召唤
核心条件：
链接怪兽必须具有增益和减益效果
箭头要求：必须使用至少一个带方向箭头的生物作为素材
费用门槛：素材生物的总费用必须大于链接生物的费用
箭头继承：链接生物继承所有素材的箭头方向
独特优势：通过链接网络将增益和减益效果作用到更多的格子，相互链接时，共享效果

注：额外怪兽不能作为额外怪兽的素材 [关键词]一种仅作用于自身的原子效果 [调整][方向箭头]均为生物标签的一种 一个生物可用具有多个标签

五、复活来源限制
`Card.WasFormallySummoned` 标记随从是否经"正式召唤"入场：普通召唤（`GameActions.PlayCard`）、额外组特招（`SummonEngine.SummonFromExtraDeck`，涵盖融合/同调/超量/连接）、
从卡组直接上场（`SearchAndPlayHandler`）、复活本身均置 `true`。被弃/被磨/送墓（hand/deck/extra→墓）的随从默认 `false`。`ReturnFromGraveyardHandler`
（复活）只能选中 `WasFormallySummoned==true` 的墓地卡——非正式召唤进墓的随从不可复活。
配套新增 `RecoverToHand`（墓→手）效果作为费用锚点（回收 1 张=1 费，蓝色）。

### 原子效果
所有卡牌效果均遵循单一数据流管道：
触发 ➔ 条件过滤 ➔ 支付代价 ➔ 锁定目标 ➔ 执行动作

1. 发动时机与条件 —— 事件驱动机制和速度系统
时机分为系统事件和原子效果事件，阶段上又分为发动，开始作用，结算完成。

2.作用目标与范围 —— 筛选与策略分离
目标选择 = 筛选器 + 决策权。
筛选器定义合法对象（如：敌方、野兽、攻击力>5），生成弹出展示筛选出的对象，玩家自行选择最终目标。
- **可选范围 + 数量两态**（无 AOE 概念）：`TargetFilter` 给可选范围，玩家在范围内指定具体目标；数量分**固定 N**（运行时须正好选 N 个）与**动态**（运行时玩家自选 0..候选数，费用计 0 且不可作地牌）。多目标对**每个目标各适用一次**效果及其后续分支（per-target）。
- **分区候选**：`TargetResolver.GetCandidates` 按 filter 中的分区 token（`Battlefield`/`Hand`/`Graveyard`/`Deck`/`Exile`）从对应区取候选；通用执行路径在候选 > 需求数时经 `TargetSelectionService.RequestAsync` 弹选择（AI/无头/超时自动取前 N）。

3. 原子效果组装 —— 节点化 per-target 条件分支
原子效果与关键词的静态数据全部由配置表驱动。
原子效果采用节点连接方式组装，运行时由 `EffectExecutor.ExecuteAsync` 遍历 `EffectDefinition.Steps`（`Effects/CardEffectConverter.cs` 在 `Steps` 非空时构建运行时步骤，空则退化扁平 `Effects` 线性结算，向后兼容）。
- **单层 + per-target 条件分支**：`then/else` 为扁平原子列表（不嵌套）；分支由 `BranchConditionEvaluator` 读**当前目标**的 `EffectExecutionContext.LastOutcome`（`EffectOutcome`：本步伤害/击杀/治疗/溢出 + `TargetLifeBefore` 快照）评估。例：「对所选 3 个目标各造成伤害 → 该目标死亡 → 抽 1」对每个死亡目标各触发一次奖励。
- **三族条件**（`Configs/BranchConfig.json` 配置驱动，`Effects/BranchConfigTable.cs` JSON 加载，增删条件不改代码）：
  - `OutcomeGate`（伤害/治疗/信息）：产出门槛达成走 `then` 奖励（**免费**）。伤害＝`TargetSurvived`/`DmgKillsTarget`/`Overkill`；治疗＝`TargetStillWounded`/`Overheal`；信息＝`TypeProphecyHit`。
  - `Drawback`（抽牌）：挂在 `DrawCard` 原子上的可叠加减费缺陷（不走 then/else）。
  - `FilterPrecision`（检索）：`SearchDeck` 的筛选即条件，按维度计费 + 从筛选范围随机抽 1。
  - 改值/改费/授予关键字/授予状态：**无可串联条件**（`GetByEffectType` 返回 null，UI 不显示「加分支」）。
- **AoE 合并**：原 `AoEDamage`/`SplitDamage` 已并入 `DealDamage` 多目标（配置 37→35），不再有独立 AOE 原子。

4.费用体系统一 —— 代价泛化，代价需要像原子效果一样可以拓展
Summon 结合游戏王特色的额外卡组特殊召唤的条件，也需要纳入代价系统
目前暂定四种，元素消耗，弃牌，扣除玩家生命，沉睡（卡翻面且增加一个苏醒倒计时），等都视为“支付代价”。发动卡牌前，需通过支付。

- ###  栈/连锁系统
- 卡的效果发动分两种，条件发动即基于事件的发动和速度发动。
- 速度发动是所有的玩家自主决定发动时机的卡和效果，都具有一个默认的发动速度，主要阶段，回合持有者1，非回合持有者0，非主要阶段，双方都是0。
- 场上有一个记速器，默认值0，双方玩家需要发动速度超过这个值，才能发动，然后入栈的同时记速器+1。双方轮询至没有人发动更高速度的效果开始结算。
结算完成一个记速器-1，但是开始结算后，不再轮询，一直到本次结算完成，记速器归0，才能进行下一次双方轮询发动效果。
-速度值来源，卡可以在编辑阶段通过添加速度相关的效果，来提高自己的发动速度，协调怪兽可以在对局中消耗额外的费用来动态提高自己的发动速度，
且协调的速度继承给用它作为素材的同步怪兽。

###
总结：这是一个基于颜色效果分类的，将原子效果组装成效果，将效果组装成卡，用卡组装成卡组，最后像普通卡牌对战游戏一样游玩的联机卡牌游戏。

---

## 项目架构

### 核心目录结构
规则引擎代码根目录为 `Assets/Scriptes/CardCore/`（约 19,600 行 / 70+ 文件）。美术资源（Synty 等）位于 `Assets/` 下其它目录，不在规则引擎工作范围内。
```
Assets/Scriptes/CardCore/
  Attribute/         # 原子效果系统 + Handlers（EffectHandlerRegistry, Handlers/*, EntityEffectExtensions）
  BaseSystems/       # 核心游戏系统（TurnEngine, CombatSystem, ElementPool, StackEngine, StateBasedActions/*）
  Cards/             # 卡牌数据模型（CardData, Card, CardWrapper, CardTypes 接口）
  Effects/           # 效果系统（EffectDefinition, CardEffectConverter, SpeedSystem, TriggerTiming, ReplacementEngine, CostSystem）
  Enums/             # 枚举定义（CardEnums, ColorIdentity, Zones）
  Events/            # 事件系统（EventManager, GameEvent）
  GameCore/          # 游戏核心（GameCore 门面, GameLoopController, GameActions, GameState, SubSystemRegistry）
  Layers/            # 层系统（LayerEngine — MTG 7 语义层唯一属性计算路径, CopyEffects, ControlChangeLayer, TextChangeLayer, ContinuousEffectDurationTracker, CharacteristicDefiningAbilities）
  Network/           # 网络消息层（NetworkMessage 信封, GameMessages DTO, NetworkSerializer）—— 仅保留线格式，未接传输
  Serialization/     # 序列化层（MemoryPack DTO, TagTable, EffectSerializer）
  Summon/            # 召唤引擎（融合/同调/超量/连接 —— 仍为 TODO）
  Utility/           # 工具类（MurmurHash3）
  ValueSystem/       # 费用和代价（SynergyConfig）
```

### 序列化架构
**双轨制**：Unity Inspector 用 `[Serializable]`/`[SerializeField]`，网络传输用 MemoryPack DTO。

| 层 | Unity 序列化 | MemoryPack 序列化 |
|---|---|---|
| 数据类 | `CardData`, `CardEffectData` | `SerializableCardData`, `SerializableCardEffectData` |
| 运行时状态 | `Card._power` 等 internal 字段 | `SerializableRuntimeCardState` |
| 效果定义 | `EffectDefinition`, `AtomicEffectInstance` | `SerializableEffectDefinition`, `SerializableAtomicEffectInstance` |
| 网络消息 | — | `NetworkMessage` 信封 + `MsgPlayCard` 等 |

`SerializableCardEffectData` 现已完整往返 `CardEffectData` 全字段（DisplayName / IsOptional / Duration / Tags / Costs / ActivationConditions / TriggerConditions / Steps），并配套嵌套 DTO `SerializableActivationConditionData` / `SerializableEffectCostEntry` / `SerializableEffectStepData`。

**MemoryPack 标签系统**：
- 属性标签：`TagTable.cs`（共 111 个 `const int`），由 `Tools/刷新属性排序` 编辑器工具（`Assets/Editor/RefreshMemoryPackOrder.cs`）从 `"ClassName.PropertyName"` 经 MurmurHash32（seed `0x53796E67`）自动生成，含碰撞检测。
- 效果标签：64 位 MurmurHash3，从效果描述文本动态生成
- 新增属性需在 `GetTagDefinitions()` 加一行（唯一真相源），点菜单刷新生成 `TagTable.cs`

**关键转换路径**：
```
JSON → CardLoader → CardData → SerializableCardData → byte[] (网络传输)
byte[] → SerializableCardData → CardData → CardWrapper (运行时)
Card → SerializableRuntimeCardState → byte[] (状态同步)
```

### 卡牌数据模型
- `CardData`：纯数据容器（ID, Supertype, CardName, Life, Power, Cost, Effects, Keywords, Tags）
- `Card`：partial class，继承 Entity，通过接口组合属性（IHasSupertype, IHasName, IHasLife 等）
- `CardWrapper`：连接 CardData 和 Card，实现所有接口
- Entity 效果扩展字段在 `EntityEffectExtensions.cs` 的 partial Card 中

### 属性计算与事件路由（唯一规范路径）
- **LayerEngine 为唯一属性计算路径**：`LayerEngine` 已重构为 MTG 标准 7 语义层（L1 Copy → L2 Control → L3 Text → L4 Type → L5 Color → L6 Ability 增删 → L7 Power/Toughness，L7 含 7a CDA→7b set→7c counters→7d ±增减→7e switch 子层）。属性查询统一走 `LayerEngine.CalculatePower/Toughness/Cost/Colors`。原 `ModifierSystem`/`AttributeCalculator`/`Unit`（旧 AttributeSystem.cs）已废弃，连续效果以 `TargetEntity` 为作用域，不再「对所有实体无差别生效」。
- **统一事件路由**：所有事件经 `GameCore.PublishEvent<T>` 单点分发——先过 `ReplacementEngine.CheckReplacements`（取 `GetFinalEvent()`），再依次 `EventManager.Publish` + `TriggerEngine.OnEvent` + `LayerEngine.OnEvent`。`TurnEngine` 不再持有私有 PublishEvent，回合/阶段事件可到达 Trigger/Layer。
- **连续效果失效为事件驱动**：UntilEndOfTurn / UntilLeaveBattlefield 由 `TurnEndEvent`/`PhaseEndEvent` 钩子驱动 `ContinuousEffectDurationTracker` 与 `TextChangeLayer` 失效，非挂钟到期。

### 配置驱动架构（唯一真相源）
原子效果与关键词的静态数据均由 JSON 配置驱动，运行时表为唯一真相源，代码侧不再保留硬编码兜底表。

**原子效果属性表**
```
Config/Attribute.xlsm → export_to_json.py → Assets/Configs/AttributeValueConfig.json → AtomicEffectTable → AtomicEffectConfig
```
- xlsm 含 `##var`/`##type` 头行，导出脚本动态读列，增列无需改 Python；数据从第 3 行起（当前 35 项，AoE/Split 已并入 DealDamage 多目标）。
- 每行除展示/费用/配色外，还含 6 列 targeting：`TargetType` / `TargetFilter` / `TargetCount` / `TargetScope` / `DurationType` / `ActivationType`。
- `AtomicEffectTable` 用 `System.IO`+`Application.dataPath` 读取（Configs 非 Resources 目录），`GetByType`/`GetByEnumName` 查询；JSON 字段解析失败才退到安全常量。
- 价值评估（`ValueSystemRuntimeConfig`）与配色（`ElementAffinity.GetAffinityForEffect`）均委托此表，不再各自维护数值/颜色硬编码。

**关键词表**
```
Assets/Configs/KeywordsConfig.json → CardLoader.LoadKeywords() → KeywordEffectMapper
```
- JSON 字段拍平到顶层（JsonUtility 不解析嵌套对象），大小写需与 DTO 严格一致。
- 区分两类效果：`atomicEffect`（被动授予的 GrantXxx）与 `triggeredEffect`（触发时执行的原子效果，搭配 `triggerTiming`/`value`/`value2`/`duration`）。`triggerTiming` 非空即判定为触发式关键词。
- `KeywordEffectMapper` 惰性委托 `CardLoader.LoadKeywords()`，无硬编码行为表。

### 回合开始接线
`TurnEngine.StartNewTurn` 发布 `TurnStartEvent`，由 `GameCore.OnTurnStarted` 统一消费：重置栈优先权持有者（`StackEngine.OnTurnStart`）+ 清零「每回合一次」使用计数（`EffectExecutor.OnNewTurn`）+ 横置恢复并发 `UntapEvent` + `ElementPool.ResetTurnUsage` + 抽 1 张（`ZoneManagerExtensions.DrawCard`）。准备阶段 `OnStandbyPhaseStarted` 自身不再做自动化，仅等待玩家放元素池后 `AdvanceFromStandby`。回合/阶段结束分别由 `GameCore.OnTurnEnded`/`OnPhaseEnded` 驱动持续效果失效。内置处理器注册统一走幂等的 `BuiltinHandlerBootstrap.EnsureRegistered()`。阶段推进与结束回合在栈非空时被守卫阻止（`CheckPhaseTransition`/`EndTurn`）。

---

## 待办事项

### 第一梯队：规则完整性剩余项
1. **回合结束手牌上限弃牌未实现**
   - 设计要求 7 张上限，`OnEndPhaseStarted()` 仍无弃牌逻辑（注释已写"手牌上限"步骤但未实现）。
2. **原子效果长尾未覆盖**
   - `AtomicEffectType` 共 115 项，已注册约 53 + 关键词；剩余长尾（Mill/Search 系细化、Equip/Attach、RandomEffect、Delayed/Repeat、部分控制/变形等）命中即 `LogWarning("未注册的效果处理器")` 静默失败。
3. **SBA 文本变更检查未注册**
   - `TextChangeChecker` 暂不注册：当前 Card 模型无可对比文本字段（需先为 Card 增文本特征）。

### 第二梯队：高级机制与验证
4. **召唤引擎未完成**
   - `SummonEngine` 中融合/同调/超量/连接的扣费、速度赋予、实例创建仍为 TODO（约 5 处），测试返回 null。本轮未纳入范围。
5. **缺少回归测试**
   - 栈/连锁（优先权传递、速度计数器、LIFO 结算）、回合循环、Layer 叠加、Replacement/SBA/Trigger 均无 EditMode 测试覆盖，正确性未经自动化验证。

### 范围外（本轮不做）
- **HexMap 六边形棋盘绑定**：HexMap 棋盘本身已独立完成，但与 CardCore 的绑定（地牌/单位落具体 HexCell、两套 `HexDirection` 对齐 link6 箭头邻接）列为独立后续任务。当前费用/元素池/召唤均为**位置无关**，地牌走 `Zone.ElementPool` 不绑定格子。
- **网络传输层**：`Network/` 仅保留线格式（`NetworkMessage`/`NetworkSerializer`），未实现 socket/同步。
- **美术表现**：不在规则引擎工作范围内。
# Tide 网络协议（M1 定版）

> 2026-09-10 定版。实现位于 `Assets/Scripts/CardCore/Network/`；本文件是线格式的唯一规范。
> 里程碑语境见 `项目概览.md` 待办章节：M1 协议定版（本文）→ M2 会话层 → M3 端到端 → M4 健壮性。

## 1. 架构定案

**权威服务器 + intent 上行 + 事件流下行**（炉石式，非确定性锁步）：

- 客户端只发 **intent**（"我想做 X"），服务器经 `NetworkIntentApplier` 分派到 `GameActions`（同步 bool 契约、失败无副作用）权威结算。
- 服务器下行 = **细粒度事件流**（`NetEventProjector` 收口 `EventManager.AnyPublished`）+ **全量快照**（`NetSnapshotBuilder`，viewerSeat 视角）+ **反问请求**（TargetSelection 网络投影）。
- 不做锁步的原因：引擎含 UniTask 异步结算、非确定容器序、洗牌随机；隐藏信息（手牌/牌库）天然需要服务端过滤。
- **训练桥（`TideHeadlessServer`，换行 JSON）冻结不动**——两条协议栈独立，互不影响。

数据驱动原则：原子效果以数据形式传输（EffectType 字符串 + 参数），协议层不硬编码原子语义——配置表/原子表增删不构成协议变更。

## 2. 帧格式与信封

**帧** = 4 字节大端长度前缀 + `MemoryPack(NetworkMessage)`（`FrameCodec`）。单帧上限 16MB。

**信封** `NetworkMessage`：

| 字段 | 类型 | 说明 |
|---|---|---|
| Type | NetworkMessageType | 见 §3 值表 |
| SequenceId | uint | 发送侧自增（`NetworkSerializer`） |
| Payload | byte[] | MemoryPack 序列化的消息体；无载荷 intent 为空 |
| Timestamp | long | UTC ticks |
| ProtocolVersion | ushort | 填 `NetworkProtocol.Version`（=1）；**读 0 视为 legacy** |

## 3. MessageType 值表（不可变契约）

**值一经分配永不重排。** 0-99 为早期草稿保留段（多为死值，仅历史占位，不再使用）。

上行 intent（客户端→服务器）：

| 值 | 类型 | Payload | 分派目标 |
|---|---|---|---|
| 100 | IntentPlayCard | MsgIntentPlayCard | 见 §5 三路合一规则 |
| 101 | IntentTapForElement | MsgIntentTapForElement | `GainElementFromToken`（颜色=玩家决策） |
| 102 | IntentAddToElementPool | MsgIntentAddToElementPool | `AddToElementPool` |
| 103 | IntentSkipStandby | — | `SkipElementPool` |
| 104 | IntentDeclareAttack | MsgIntentDeclareAttack | `DeclareAttack` |
| 105 | IntentDeclareBlock | MsgIntentDeclareBlock | `CombatSystem.DeclareBlock`（**预留**：引擎有 API 零调用） |
| 106 | IntentActivateEffect | MsgIntentActivateEffect | `ActivateEffect`/PendingEffect 带目标路径 |
| 107 | IntentPassPriority | — | `PassPriority` |
| 108 | IntentEndTurn | — | `EndTurn` |
| 109 | IntentConcede | — | `EndGame(opponent, Concede)` |
| 110 | SelectResponse | MsgSelectResponse | 路由到 `NetworkTargetSelector.TryComplete`（**不经 intent 分派口**） |
| 111 | DeckSubmit | MsgDeckSubmit | 开局握手（对局前，§11）——`NetMatchHandshake.ValidateDeckSubmit`，拒绝走 Error 帧 |

下行（服务器→客户端）：

| 值 | 类型 | Payload |
|---|---|---|
| 120 | SelectRequest | MsgSelectRequest |
| 121 | NetEventBatch | MsgNetEventBatch |
| 122 | GameStateSyncV2 | MsgGameStateSync |
| 123/124 | Ping/Pong | — |
| 125 | MatchManifest | MsgMatchManifest（开局握手，§11） |

## 4. 实体引用（NetEntityRef）

双轨表达：**卡 = RuntimeId**（`Entity` 构造期 `Interlocked` 自增，进程内唯一、跨局不复用）；**玩家 = 座位号**（P1=0/P2=1）。

- `CardId` 字段是卡表模板 ID，仅供客户端查表显示——**不可当实例身份**（`BuildDeck` 副本共享模板 ID）。
- 为什么不用 Card.ID / 时间戳：Card.ID 唯一赋值点是模板注入；Card 构造 `createTimestamp:false`，Sequence 恒 0；AI 观测层是槽位制无跨时点身份。三条路都不通，故新增 RuntimeId。
- 服务器反向解析：`NetEntityDirectory`（RuntimeId→Entity 缓存，miss 全区域扫描 ≤200 张）。

## 5. 上行 intent 语义

**PlayCard 三路合一**：`FromZone = Hand/Graveyard`。服务器分派规则——来源为手牌 且 栈非空 且 非结算中 且 请求者持优先权 → `PlayCardInResponse`（响应出牌）；否则 `PlayCard`（主阶段+回合玩家门禁，Graveyard 走归土仪典配额）。分派只是路由，全部校验仍在引擎入口。

**ActivateEffect 寻址**：`EffectId = EffectDefinition.Id`（稳定键）。**不是** EffectTag——那是 Description 的 MurmurHash 内容指纹，仅序列化侧使用。空 Id 回退 `"EFF_{cardId}"`（CardEffectConverter 口径）。

**拒绝语义**：`NetworkIntentApplier.Apply` 返回 false + 原因字符串（实体解析失败 / 引擎门禁拒绝），传输层包成 Error 帧回发（M2）。

**客户端感知"轮到我"**：事件流 `StackAdd` / `PriorityGain` / `PriorityPass` / `StackResolutionStart/End` / `StackEmpty` + 快照 `PrioritySeat` 双通道。

## 6. 下行：事件流

**通用事件信封（schema-less）**——引擎事件是 ~109 个 CLR 类且持续演化，不做逐类手写 DTO：

```
NetEvent { EventType(类型名，跨版本稳定); EventId(引擎自增); TurnNumber; NetParam[] }
NetParam { FieldName(与事件属性同名); Kind; IntValue; FloatValue; StringValue; EntityRefs[] }
```

投影规则（`NetEventProjector`，反射 + PropertyInfo 按类型缓存）：

| 属性形态 | Kind | 承载 |
|---|---|---|
| null | Null | （字段存在但值为空） |
| bool / 整型 / DateTime | Int | IntValue（DateTime 存 Ticks） |
| float / double / decimal | Float | FloatValue |
| string | String | StringValue |
| 枚举 | Enum | IntValue=数值 + StringValue=枚举名 |
| Card / Player | Entity | EntityRefs[0] |
| 实体集合（List\<Card\> 等） | EntityList | EntityRefs[N]（混合元素整字段降级 Opaque） |
| 嵌套 IGameEvent | EventRef | IntValue=其 EventId（**不递归**：防环 + 嵌套事件已单独投过） |
| Effect / EffectInstance / Context 等 | Opaque | StringValue=ToString()，**仅展示用途，客户端真身以快照为准** |

**演进语义**：新增事件/字段不是破坏性变更——新客户端读新参数，旧客户端忽略未知 FieldName。

**线上去文本（2026-09-22 定案，与三层 ID 引用同口径）**：事件属性可标 `[NetProjectionIgnore]`——投影期剔除、不进线格式（本地 CLR 订阅者不受影响）。现役标注：`EffectExecutionSummaryEvent.Description/Instance`（服务器拼好的战报文本与不可结构化重对象）——该事件线上只传 `EffectId`(String) / `Source`(Entity) / `Controller`(Entity)，客户端按 EffectId 查 Effects.json + 原子表（AtomText 模板）本地重渲染；真实数值（随机实掷等）由伤害/治疗等细粒度事件承载。

**隐藏信息口径（重要）**：`NetEvent` 是**全信息内部流**（含 `CardDrawEvent.DrawnCard`、对方入手等）——按座位过滤发生在**服务器出队时**（M2 实现传输层过滤），投影器本身不做裁剪。

收口点：`EventManager.AnyPublished`（恰一次、保序、先于 handler），挂载于 `GameCore.Reset`（与 MatchLogService 同款）。

## 7. 下行：全量快照（GameStateSyncV2）

`MsgGameStateSync`（`NetSnapshotBuilder.Build(core, viewerSeat)`）：

- 座位三值：ViewerSeat / ActiveSeat / PrioritySeat。
- `PlayerState`：生命/生命上限/牌库数/手牌数/疲劳/地牌槽上限/bank 各色量（含黑/白——2026-09-11 转正，卡结算产生）/抵消用量（四机制，兑换产黑计数）/坟场数/除外数/IsAI。
- `ZoneCards[]`：公开区全量（战场/坟场/除外/元素池/发动区/场地区/额外卡组）+ viewer 自己的手牌；**牌库不进**（只数量）。
- `Hands[]`（CardHandInfo）：Count 恒有；**OwnRuntimeIds 仅填 viewer 自己**（对方为空数组）。
- `StackV2[]`（StackItemDTO）：栈条目 = EffectInstance 投影（Source 引用/IsCardCast/EffectId/ModeIndex/Targets/速度/栈对象类型），数组末尾为栈顶。效果显示名不上网——客户端按 EffectId 查 Effects.json（2026-09-22 线上去文本；EffectDisplayName 字段已删，tag 保留位）。旧 `Stack`(SerializableEffectDefinition[]) 字段与全量效果定义 DTO 已整体删除（同日，效果定义不上网、按 EffectId 引用；tag 保留位）。
- 卡状态 `SerializableRuntimeCardState`：含 RuntimeId / ControllerSeat / IsFrozen（快照对账口径，单向导出不回灌）。
- **字节稳定**：Counters 按键排序——同状态两次序列化 byte[] 全等（确定性回归锚点）。

**时点约束（M2 服务器收口）**：人局 EndTurn 后可能赶上半完成结算（EnforceHandLimitAsync / PassPriority 内部异步）——服务器快照前应静默检查（IsResolving + 栈空 + 无 pending）再取样。

## 8. 下行/上行：反问协议（选择交互）

引擎全部结算期交互（目标选择/抉择 mode/弃牌/召唤素材/代价抵消/重定向）经 `TargetSelectionService` 单漏斗，网络投影为：

- 服务器→客户端 `MsgSelectRequest`：RequestId / ChooserSeat / Title / Hint / AllowCancel / TimeoutSeconds / Min / Max / Labels[] / Candidates[]。**实体反问（Candidates 非空）Labels 恒空**——卡名客户端按 CardId 查表渲染（2026-09-22 线上去文本，应答索引指向 Candidates 下标不变）；纯选项反问时 Candidates 空、Labels 为选项文本（语义载荷）。
- 客户端→服务器 `MsgSelectResponse`：RequestId / Indices[]（指向 Labels/Candidates 下标；越界/超量由引擎既有 AutoSelect 兜底）。
- 引擎侧实现：`NetworkTargetSelector : ITargetSelectorEx`（requestId→TCS 关联；`TargetSelectionService.Current` 分流，`UiTargetSelector` 零改动）。
- 超时：引擎 GraceSeconds 安全网原样生效 = 超时代打走 AutoSelect（M4 再做服务器主动代打）。
- **AI 座位不产生网络反问**（`Chooser.IsAI` → 本地 AutoSelect）。

## 9. 已知限制（如实声明）

1. **PositionPicker 未网络化**：预言卡位选择是同步委托（同步 Execute 内调用），网络往返无法阻塞——M1 保持缺省行为（首个未展示位），SelectResponse 已为其预留；异步化 = 该 handler 改走 ExecuteAsync 覆写，记 M2。
2. MorphSystem 目标解析是卡表查询非玩家决策——服务器本地注入（`CardCatalog.GetById`），不经协议。
3. 投影 Opaque 字段有损（Effect/Context 类）——客户端语义真身依赖快照。
4. 线程模型：M1 回环全程主线程；M2 起服务器必须**单逻辑线程**驱动（intent 入队、逻辑线程消费），RuntimeId 已线程安全、投影器追加已加锁。
5. 持续时限 buff（DurationTracker）/装备贴附关系未进快照——M2+ 扩展位。

## 10. 验证

- **回环验证器**：Unity 菜单 `Tools/网络协议回环验证`（`Assets/Editor/验证/NetProtocolLoopbackVerifier.cs`）——整局 AI 对战跑通 ①事件投影完整无损（与 MatchLogService 同量、EventId 严格递增、信封+帧往返深度比较、半包/粘包切分）+线上去文本口径（执行摘要事件只传 EffectId/Source/Controller、实体反问 Labels 恒空）②快照双视角对账（活体全等、隐藏信息口径、字节确定性、中段栈快照）③intent 通道覆盖（PlayCard/PlayLand/Attack/EndTurn/Concede 经完整协议栈分派）④反问回环（真实引擎调用点走 Ex 分支、索引→实体映射、卸载后 headless 路径不变）⑤开局握手（闭包摘要确定性与非全量口径、正例全链路往返+双座位双向校验、缺卡/重复卡/原子行摘要漂移/缺摘要/回显篡改全部拒绝且文案命中）。
- 构建门禁：`dotnet build Assembly-CSharp.csproj` / `Assembly-CSharp-Editor.csproj` 0 错误。
- 新增 DTO 流程：`RefreshMemoryPackOrder.GetTagDefinitions()` 登记 → 跑 `Tools/刷新属性排序`（手算同值可先行——tag = MurmurHash32(UTF8("类.字段"), 0x53796E67) & 0x7FFFFFFF）。

## 11. 开局握手（对局前卡组校验，2026-09-22）

**只核对卡组引用闭包，不做全池对比**——不比对整个 Cards.json/Effects.json（无"共享牌库版本"概念，双方卡池可以不同）。缺卡/缺效果/缺原子行开局拒绝，不再静默打成"缺卡对局"；对手侧与未来更广的校验遵循**用到再核对**原则（对局中按实际出现的 ID/行核对），开局一律不做全量对比。

**一致性依据**：卡/效果 ID 本身是内容哈希——**ID 命中即内容一致**，无需内容摘要；唯一需要内容核对的是**闭包引用到的原子表行**（行 ID 稳定、内容是平衡层可变）。

**消息流**（M2 会话层驱动；M1 定消息 + `NetMatchHandshake` 纯逻辑，回环验证器覆盖）：

1. 客户端 → 服务器 `DeckSubmit`(111)：`DeckName / CardIds[]（Cards.json 卡 ID）/ Digest(NetDeckDigest)`。
2. 服务器 `ValidateDeckSubmit`：卡表全命中 + 卡组不重复（README 铁律）+ 效果/原子引用闭环（effectIds → Effects.json；步内/代价 payload/奖励的 refId → 原子表——装载期静默告警在此显性拦截）+ 客户端摘要与服务器**按同一卡组**计算的摘要比对。任一失败 = **Error 帧(99) 拒绝**（复用 intent 拒绝语义，reason 指明问题）。
3. 双座位通过后，服务器 → 各客户端 `MatchManifest`(125)：`OwnSeat / OwnCardIds（己方卡组回显，与提交序一致）/ OpponentCardCount（对手只给数量——卡组构成是隐藏信息）/ OwnDeckDigest（服务器按该座位卡组计算的摘要）`。
4. 客户端 `VerifyMatchManifest`：己方回显对账 + 服务器摘要 vs 本地按同一卡组计算的摘要（双向握手）；失败本地中止进局。

**NetDeckDigest 口径**——卡组引用闭包内全部原子表行的内容摘要：

| 字段 | 口径 |
|---|---|
| AtomicRowsHash | 闭包 refId 集（卡→effectIds→效果步/代价 payload/奖励）对应表行内容（费用构成/极性/目标域/可装载域/模板文案，排序后哈希） |
| AtomicRowCount | 闭包引用行数（仅供拒绝文案诊断） |
